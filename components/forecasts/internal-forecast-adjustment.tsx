"use client"

import { useState } from "react"
import { Lock, Save } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog"
import { Input } from "@/components/ui/input"
import { toast } from "sonner"

type Props = { open: boolean; onOpenChange: (open: boolean) => void; partnerName?: string; techCompanyName?: string; declaredRevenue?: number }

export function InternalForecastAdjustment({ open, onOpenChange, partnerName = "Partner", techCompanyName = "TechCompany", declaredRevenue = 30000 }: Props) {
  const [internalTarget, setInternalTarget] = useState(String(declaredRevenue))
  const [displayTarget, setDisplayTarget] = useState("scaleup_internal")
  const save = () => { toast.success("Ajuste preparado. La persistencia requiere habilitar la integración de datos."); onOpenChange(false) }
  return <Dialog open={open} onOpenChange={onOpenChange}><DialogContent className="max-w-2xl"><DialogHeader><div className="flex flex-wrap gap-2"><Badge variant="secondary">Vista de Administración Interna</Badge><Badge variant="outline">Período: 2026</Badge><Badge variant="outline">Solo Admin / Acceso Restringido</Badge></div><DialogTitle className="pt-2">Ajuste Interno de Forecast</DialogTitle><DialogDescription>Configura la expectativa realista de ScaleUP para {partnerName} × {techCompanyName}.</DialogDescription></DialogHeader><div className="flex flex-col gap-6"><div className="grid gap-4 md:grid-cols-2"><Card className="bg-muted/40"><CardHeader><CardTitle className="flex items-center gap-2 text-base"><Lock data-icon="inline-start" />Compromiso del Partner</CardTitle><CardDescription>Monto acordado en la sesión de alineación anual.</CardDescription></CardHeader><CardContent><p className="text-2xl font-semibold">${declaredRevenue.toLocaleString("en-US")} <span className="text-sm font-normal text-muted-foreground">USD</span></p></CardContent></Card><Card><CardHeader><CardTitle className="text-base">Forecast Realista ScaleUP</CardTitle><CardDescription>Proyección interna definida por el administrador.</CardDescription></CardHeader><CardContent><div className="relative"><span className="pointer-events-none absolute left-3 top-2.5 text-muted-foreground">$</span><Input aria-label="Forecast Realista ScaleUP" className="pl-7" inputMode="decimal" min="0" type="number" value={internalTarget} onChange={(event) => setInternalTarget(event.target.value)} /><span className="pointer-events-none absolute right-3 top-2.5 text-sm text-muted-foreground">USD</span></div></CardContent></Card></div><Card className="border-primary/20 bg-primary/5"><CardHeader><CardTitle className="text-base">Target Visible en Reportes de TechCompany</CardTitle><CardDescription>Define qué valor verán por defecto los reportes externos.</CardDescription></CardHeader><CardContent><div className="flex flex-col gap-3"><label className="flex cursor-pointer items-start gap-3 rounded-lg border bg-background p-3"><input type="radio" name="display-target" value="scaleup_internal" checked={displayTarget === "scaleup_internal"} onChange={(event) => setDisplayTarget(event.target.value)} className="mt-1" /><span><span className="flex items-center gap-2 font-medium">Forecast Realista ScaleUP <Badge variant="secondary">Recomendado</Badge></span><span className="text-sm text-muted-foreground">Muestra el monto ajustado por el Admin.</span></span></label><label className="flex cursor-pointer items-start gap-3 rounded-lg border bg-background p-3"><input type="radio" name="display-target" value="partner_target" checked={displayTarget === "partner_target"} onChange={(event) => setDisplayTarget(event.target.value)} className="mt-1" /><span><span className="font-medium">Target Declarado del Partner</span><span className="block text-sm text-muted-foreground">Muestra el monto original del Partner.</span></span></label></div></CardContent></Card></div><p className="text-xs text-muted-foreground">Último ajuste por Matias Schwartz (Admin) · Hace 2 días</p><DialogFooter><Button variant="outline" onClick={() => onOpenChange(false)}>Cancelar</Button><Button onClick={save}><Save data-icon="inline-start" />Guardar Ajuste Interno</Button></DialogFooter></DialogContent></Dialog>
}
